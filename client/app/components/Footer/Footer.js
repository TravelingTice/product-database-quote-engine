import React, { Component } from 'react';
import { setInStorage } from '../../utils/storage';

import { signOut } from '../../utils/AccountAPI';

class Footer extends Component {
  constructor(props) {
    super(props)

  }

  render() {
    return (
      <footer>
      </footer>
    )
  }
}

export default Footer;
